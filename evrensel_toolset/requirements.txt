flask
pyyaml
pywebview
Pillow
numpy

# Model Eğitimi / Model Test için (GPU kullanacaksan PyTorch'u CUDA'lı
# sürümüyle ELLE kur, ultralytics'in otomatik kuracağı CPU'lu sürüm
# GPU'nu kullanmaz):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install ultralytics
ultralytics
opencv-python
imageio-ffmpeg

# Fusion aracı İÇİN GEREKLİ (bu kopyada varsayılan KAPALI -- ana sayfadaki
# "Gelişmiş özellikler" anahtarını açmadıysan bu iki paketi kurmana hiç
# gerek yok). Açacaksan yorum satırından çıkarıp kur:
#   NVIDIA GPU'n varsa: pip install fast-plate-ocr[onnx-gpu]
#   Sadece CPU ile: pip install fast-plate-ocr[onnx]
# fast-plate-ocr[onnx]
# pillow-heif
